import './Header.css';
import React from 'react';

function Header() {
    return (
        <header>
            <span className="logo">Best AI</span>
        </header>
    );
}

export default Header;